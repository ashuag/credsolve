/** Written to `lead.lead_status_note` when penny-drop fails (legacy copy still matched). */
export const BANK_DETAIL_FAILED_NOTE = 'Bank detail failed';

/** Canonical note / rejection-reason label for penny-drop failure. */
export const PENNY_DROP_FAILED_NOTE = 'Penny drop failed';

/** Written to `application.application_status_note` while credit reviews a bank name mismatch. */
export const BANK_NAME_REVIEW_NOTE = 'Bank name mismatch pending credit review';
